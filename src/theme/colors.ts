import { getThemeColors } from "../utils";

const brandColor = getThemeColors({});

export const colors = {
  
 
  bg: {
    white: "hsl(0, 0%, 98%)",
    gray: "hsl(0, 0%, 41%)",
    black: "hsl(0, 0%, 8%)",
    
  },
  
};
